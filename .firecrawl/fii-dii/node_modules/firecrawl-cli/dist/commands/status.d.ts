/**
 * Status command implementation
 * Displays CLI version, auth status, concurrency, and credits
 */
export type AuthSource = 'env' | 'stored' | 'none';
interface StatusResult {
    version: string;
    authenticated: boolean;
    authSource: AuthSource;
    concurrency?: {
        active: number;
        max: number;
    };
    credits?: {
        remaining: number;
        plan: number;
    };
    error?: string;
}
/**
 * Detect how the user is authenticated
 */
export declare function getAuthSource(): AuthSource;
/**
 * Get full status information
 */
export declare function getStatus(): Promise<StatusResult>;
/**
 * Handle status command output
 */
export declare function handleStatusCommand(): Promise<void>;
export {};
//# sourceMappingURL=status.d.ts.map