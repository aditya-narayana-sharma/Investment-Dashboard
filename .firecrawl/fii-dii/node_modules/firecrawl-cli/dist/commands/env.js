"use strict";
/**
 * Env command implementation
 * Pull authenticated environment variables into project .env files
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleEnvPullCommand = handleEnvPullCommand;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const config_1 = require("../utils/config");
const auth_1 = require("../utils/auth");
/**
 * Pull FIRECRAWL_API_KEY into a local .env file
 */
async function handleEnvPullCommand(options = {}) {
    const apiKey = (0, config_1.getApiKey)() || (await (0, auth_1.ensureAuthenticated)());
    const envFile = options.file || '.env';
    const envPath = path.resolve(process.cwd(), envFile);
    const envKey = 'FIRECRAWL_API_KEY';
    const envLine = `${envKey}=${apiKey}`;
    if (fs.existsSync(envPath)) {
        const contents = fs.readFileSync(envPath, 'utf-8');
        const lines = contents.split('\n');
        const existingIndex = lines.findIndex((line) => line.startsWith(`${envKey}=`));
        if (existingIndex !== -1) {
            if (!options.overwrite) {
                console.log(`${envKey} already exists in ${envFile}. Use --overwrite to replace it.`);
                return;
            }
            lines[existingIndex] = envLine;
            fs.writeFileSync(envPath, lines.join('\n'), 'utf-8');
            console.log(`✓ Updated ${envKey} in ${envFile}`);
        }
        else {
            const separator = contents.endsWith('\n') ? '' : '\n';
            fs.appendFileSync(envPath, `${separator}${envLine}\n`, 'utf-8');
            console.log(`✓ Added ${envKey} to ${envFile}`);
        }
    }
    else {
        fs.writeFileSync(envPath, `${envLine}\n`, 'utf-8');
        console.log(`✓ Created ${envFile} with ${envKey}`);
    }
}
//# sourceMappingURL=env.js.map