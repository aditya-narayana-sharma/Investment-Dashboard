"use strict";
/**
 * Interact session persistence utility
 * Stores the last scrape ID so interact commands can reuse it automatically
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveInteractSession = saveInteractSession;
exports.loadInteractSession = loadInteractSession;
exports.clearInteractSession = clearInteractSession;
exports.getScrapeId = getScrapeId;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const os = __importStar(require("os"));
function getConfigDir() {
    const homeDir = os.homedir();
    const platform = os.platform();
    switch (platform) {
        case 'darwin':
            return path.join(homeDir, 'Library', 'Application Support', 'firecrawl-cli');
        case 'win32':
            return path.join(homeDir, 'AppData', 'Roaming', 'firecrawl-cli');
        default:
            return path.join(homeDir, '.config', 'firecrawl-cli');
    }
}
function getSessionPath() {
    return path.join(getConfigDir(), 'interact-session.json');
}
function ensureConfigDir() {
    const configDir = getConfigDir();
    if (!fs.existsSync(configDir)) {
        fs.mkdirSync(configDir, { recursive: true, mode: 0o700 });
    }
}
function saveInteractSession(session) {
    ensureConfigDir();
    fs.writeFileSync(getSessionPath(), JSON.stringify(session, null, 2), 'utf-8');
}
function loadInteractSession() {
    try {
        const sessionPath = getSessionPath();
        if (!fs.existsSync(sessionPath)) {
            return null;
        }
        return JSON.parse(fs.readFileSync(sessionPath, 'utf-8'));
    }
    catch {
        return null;
    }
}
function clearInteractSession() {
    try {
        const sessionPath = getSessionPath();
        if (fs.existsSync(sessionPath)) {
            fs.unlinkSync(sessionPath);
        }
    }
    catch {
        // Ignore errors
    }
}
const SESSION_STALE_MS = 10 * 60 * 1000; // 10 minutes
/**
 * Resolve scrape ID from explicit override or stored session
 */
function getScrapeId(overrideId) {
    if (overrideId)
        return overrideId;
    const stored = loadInteractSession();
    if (stored) {
        const ageMs = Date.now() - new Date(stored.createdAt).getTime();
        if (ageMs > SESSION_STALE_MS) {
            const mins = Math.round(ageMs / 60_000);
            process.stderr.write(`Warning: Last scrape session is ${mins}m old and may have expired. ` +
                `Re-scrape or pass --scrape-id explicitly.\n`);
        }
        return stored.scrapeId;
    }
    throw new Error('No active scrape session. Scrape a URL first:\n' +
        '  firecrawl scrape https://example.com\n' +
        'Or specify a scrape ID explicitly:\n' +
        '  firecrawl interact <scrape-id> "prompt"');
}
//# sourceMappingURL=interact-session.js.map