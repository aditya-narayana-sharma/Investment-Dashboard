/**
 * Setup command implementation
 * Installs firecrawl skill files and MCP server into AI coding agents
 */
export type SetupSubcommand = 'skills' | 'workflows' | 'mcp' | 'defaults';
export interface SetupOptions {
    global?: boolean;
    agent?: string;
    undo?: boolean;
    /** Skip the interactive harness picker and apply to all agents. */
    yes?: boolean;
    /** Use the built-in skill installer instead of shelling out to npx skills. */
    nativeSkills?: boolean;
    /** Render compact skill install output. */
    quiet?: boolean;
}
/**
 * Main setup command handler
 */
export declare function handleSetupCommand(subcommand?: SetupSubcommand, options?: SetupOptions): Promise<void>;
export declare function handleMakeDefaultCommand(options?: SetupOptions): Promise<void>;
export declare function installSkillsForAgent(agent: string, options?: SetupOptions, repos?: readonly string[]): Promise<void>;
export declare function installMcp(options: SetupOptions): Promise<void>;
export declare function installHermesMcp(): Promise<void>;
export declare function installOpenClawMcp(): Promise<void>;
//# sourceMappingURL=setup.d.ts.map