# Integrations

## Zerodha Kite

The dashboard's active runtime integration is the Go MCP server in the adjacent
repository:

`/Users/adityasharma/Documents/GitHub/kite-mcp-server`

The `kite-connect-mcp-typescript/` directory is the earlier TypeScript prototype.
It is retained for reference and portability, but is not started by the dashboard.
Use `KITE_MCP_PROJECT_DIR` when the active Go server is installed elsewhere.
