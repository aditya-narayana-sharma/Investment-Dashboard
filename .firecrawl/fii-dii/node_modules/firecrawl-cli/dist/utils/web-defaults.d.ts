export type WebAgent = 'Claude Code' | 'Codex';
export declare const WEB_AGENTS: readonly WebAgent[];
export interface WebDefaultsOptions {
    undo?: boolean;
    /** Limit configuration to these agents. Defaults to all agents. */
    agents?: readonly WebAgent[];
}
export interface WebDefaultResult {
    agent: WebAgent;
    path: string;
    changed: boolean;
    skipped?: boolean;
    message: string;
}
export declare function configureWebDefaults(options?: WebDefaultsOptions): Promise<WebDefaultResult[]>;
//# sourceMappingURL=web-defaults.d.ts.map