"use strict";
/**
 * Tests for the doctor command. Covers:
 *  - version comparison utility
 *  - MCP-entry detection helper
 *  - runChecks() across pass/warn/fail scenarios
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const fs_1 = __importDefault(require("fs"));
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
const npm_registry_1 = require("../../utils/npm-registry");
const agents_1 = require("../../utils/agents");
const doctor_1 = require("../../commands/doctor");
const config_1 = require("../../utils/config");
const mockFetch = vitest_1.vi.fn();
global.fetch = mockFetch;
(0, vitest_1.describe)('compareVersions', () => {
    (0, vitest_1.it)('returns 0 for equal versions', () => {
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('1.2.3', '1.2.3')).toBe(0);
    });
    (0, vitest_1.it)('returns negative when first is older', () => {
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('1.0.0', '1.0.1')).toBeLessThan(0);
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('1.0.0', '2.0.0')).toBeLessThan(0);
    });
    (0, vitest_1.it)('returns positive when first is newer', () => {
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('1.0.1', '1.0.0')).toBeGreaterThan(0);
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('2.0.0', '1.9.9')).toBeGreaterThan(0);
    });
    (0, vitest_1.it)('handles missing parts and v-prefix', () => {
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('v1.2', '1.2.0')).toBe(0);
        (0, vitest_1.expect)((0, npm_registry_1.compareVersions)('1.2.0-beta.1', '1.2.0')).toBe(0);
    });
});
(0, vitest_1.describe)('hasFirecrawlMcpEntry', () => {
    (0, vitest_1.it)('detects firecrawl under mcpServers', () => {
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)({
            mcpServers: { firecrawl: { command: 'npx' } },
        })).toBe(true);
    });
    (0, vitest_1.it)('detects firecrawl under mcp.servers (VS Code style)', () => {
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)({
            mcp: { servers: { firecrawl: { command: 'npx' } } },
        })).toBe(true);
    });
    (0, vitest_1.it)('returns false when no firecrawl entry present', () => {
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)({
            mcpServers: { other: { command: 'npx' } },
        })).toBe(false);
    });
    (0, vitest_1.it)('returns false for non-object input', () => {
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)(null)).toBe(false);
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)('string')).toBe(false);
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)(42)).toBe(false);
    });
    (0, vitest_1.it)('walks nested objects', () => {
        (0, vitest_1.expect)((0, agents_1.hasFirecrawlMcpEntry)({
            projects: {
                '/repo': { mcpServers: { firecrawl: {} } },
            },
        })).toBe(true);
    });
});
(0, vitest_1.describe)('runChecks', () => {
    let tmpCwd;
    let originalCwd;
    (0, vitest_1.beforeEach)(() => {
        (0, config_1.resetConfig)();
        mockFetch.mockReset();
        originalCwd = process.cwd();
        tmpCwd = fs_1.default.mkdtempSync(path_1.default.join(os_1.default.tmpdir(), 'doctor-test-'));
        process.chdir(tmpCwd);
    });
    (0, vitest_1.afterEach)(() => {
        process.chdir(originalCwd);
        fs_1.default.rmSync(tmpCwd, { recursive: true, force: true });
        (0, config_1.resetConfig)();
    });
    /**
     * Stub every outbound fetch the doctor makes.
     *  - registry.npmjs.org    → returns { version }
     *  - /v2/team/credit-usage → returns credit usage payload + status
     *  - /v2/team/queue-status → returns queue payload + status
     */
    function stubFetch(opts) {
        mockFetch.mockImplementation(async (url) => {
            if (url.includes('registry.npmjs.org')) {
                if (opts.registryUnreachable)
                    throw new Error('ENOTFOUND');
                return {
                    ok: true,
                    status: 200,
                    json: async () => ({ version: opts.latestVersion ?? '1.0.0' }),
                };
            }
            if (url.includes('/v2/team/credit-usage')) {
                if (opts.slowMs)
                    await new Promise((r) => setTimeout(r, opts.slowMs));
                const status = opts.creditsStatus ?? 200;
                return {
                    ok: status >= 200 && status < 300,
                    status,
                    json: async () => ({ data: opts.credits ?? {} }),
                };
            }
            if (url.includes('/v2/team/queue-status')) {
                const status = opts.queueStatus ?? 200;
                return {
                    ok: status >= 200 && status < 300,
                    status,
                    json: async () => ({
                        success: true,
                        activeJobsInQueue: 0,
                        maxConcurrency: 10,
                        ...opts.queue,
                    }),
                };
            }
            throw new Error(`Unexpected fetch: ${url}`);
        });
    }
    function checkByName(checks, name) {
        const c = checks.find((x) => x.name === name);
        if (!c)
            throw new Error(`Missing check: ${name}`);
        return c;
    }
    (0, vitest_1.it)('fails the API Key check when no key is configured', async () => {
        stubFetch({});
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'API Key').status).toBe('fail');
        // Without a key we don't ping the API, so concurrency comes back as fail
        (0, vitest_1.expect)(checkByName(checks, 'Concurrency').status).toBe('fail');
    });
    (0, vitest_1.it)('passes the happy path with current version and full credits', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-abc123def456',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            latestVersion: '0.0.1', // older than packageJson, so doctor sees pass
            credits: { remainingCredits: 9000, planCredits: 10000 },
            queue: { success: true, activeJobsInQueue: 1, maxConcurrency: 10 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'API Key').status).toBe('pass');
        (0, vitest_1.expect)(checkByName(checks, 'API Reachability').status).toBe('pass');
        (0, vitest_1.expect)(checkByName(checks, 'API Key Validity').status).toBe('pass');
        (0, vitest_1.expect)(checkByName(checks, 'Credits').status).toBe('pass');
        (0, vitest_1.expect)(checkByName(checks, 'Concurrency').status).toBe('pass');
    });
    (0, vitest_1.it)('labels API keys passed by flag as flag sourced', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-stored',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            credits: { remainingCredits: 500, planCredits: 1000 },
        });
        const { checks } = await (0, doctor_1.runChecks)({ apiKey: 'fc-flag' });
        (0, vitest_1.expect)(checkByName(checks, 'API Key').message).toBe('fc-...flag (flag)');
    });
    (0, vitest_1.it)('warns on outdated CLI version', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            latestVersion: '99.99.99',
            credits: { remainingCredits: 100, planCredits: 1000 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'CLI Version').status).toBe('warn');
    });
    (0, vitest_1.it)('fails API Key Validity on 401', async () => {
        (0, config_1.initializeConfig)({ apiKey: 'fc-bad', apiUrl: 'https://api.firecrawl.dev' });
        stubFetch({ creditsStatus: 401 });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'API Key Validity').status).toBe('fail');
    });
    (0, vitest_1.it)('warns when credits are below 10% of plan', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            credits: { remainingCredits: 50, planCredits: 1000 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'Credits').status).toBe('warn');
    });
    (0, vitest_1.it)('does not render impossible credit percentages above 100%', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            credits: { remainingCredits: 5000, planCredits: 1000 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'Credits')).toMatchObject({
            status: 'pass',
            message: '5,000 / 1,000 (above plan)',
        });
    });
    (0, vitest_1.it)('fails when credits hit zero', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            credits: { remainingCredits: 0, planCredits: 1000 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'Credits').status).toBe('fail');
    });
    (0, vitest_1.it)('warns concurrency when active >= max', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            credits: { remainingCredits: 500, planCredits: 1000 },
            queue: { success: true, activeJobsInQueue: 10, maxConcurrency: 10 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'Concurrency').status).toBe('warn');
    });
    (0, vitest_1.it)('warns when .firecrawl/ exists but .gitignore is missing', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        fs_1.default.mkdirSync(path_1.default.join(tmpCwd, '.firecrawl'));
        stubFetch({ credits: { remainingCredits: 500, planCredits: 1000 } });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, '.gitignore').status).toBe('warn');
    });
    (0, vitest_1.it)('passes when .firecrawl/ exists and is gitignored', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        fs_1.default.mkdirSync(path_1.default.join(tmpCwd, '.firecrawl'));
        fs_1.default.writeFileSync(path_1.default.join(tmpCwd, '.gitignore'), '.firecrawl/\n');
        stubFetch({ credits: { remainingCredits: 500, planCredits: 1000 } });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, '.gitignore').status).toBe('pass');
    });
    (0, vitest_1.it)('warns when .env key mismatches configured key', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-stored',
            apiUrl: 'https://api.firecrawl.dev',
        });
        fs_1.default.writeFileSync(path_1.default.join(tmpCwd, '.env'), 'FIRECRAWL_API_KEY=fc-different\n');
        stubFetch({ credits: { remainingCredits: 500, planCredits: 1000 } });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'Local .env').status).toBe('warn');
    });
    (0, vitest_1.it)('warns on custom (non-default) API URL', async () => {
        (0, config_1.initializeConfig)({ apiKey: 'fc-test', apiUrl: 'http://localhost:3002' });
        stubFetch({ credits: { remainingCredits: 500, planCredits: 1000 } });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'API Reachability').status).toBe('warn');
    });
    (0, vitest_1.it)('does not warn on the default API URL with a trailing slash', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev/',
        });
        stubFetch({ credits: { remainingCredits: 500, planCredits: 1000 } });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(checkByName(checks, 'API Reachability').status).toBe('pass');
    });
    (0, vitest_1.it)('returns plain messages without ANSI escapes for JSON output', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        stubFetch({
            latestVersion: '99.99.99',
            credits: { remainingCredits: 500, planCredits: 1000 },
        });
        const { checks } = await (0, doctor_1.runChecks)({});
        (0, vitest_1.expect)(JSON.stringify({ checks })).not.toMatch(/\u001b\[/);
        (0, vitest_1.expect)(checkByName(checks, 'CLI Version').message).toContain('(v99.99.99 available)');
    });
});
(0, vitest_1.describe)('runSupportAsk', () => {
    const jobId = '123e4567-e89b-12d3-a456-426614174000';
    (0, vitest_1.beforeEach)(() => {
        (0, config_1.resetConfig)();
        mockFetch.mockReset();
        vitest_1.vi.spyOn(console, 'log').mockImplementation(() => { });
        vitest_1.vi.spyOn(console, 'error').mockImplementation(() => { });
    });
    (0, vitest_1.afterEach)(() => {
        vitest_1.vi.restoreAllMocks();
        (0, config_1.resetConfig)();
    });
    (0, vitest_1.it)('posts question and jobId to the support Ask endpoint', async () => {
        (0, config_1.initializeConfig)({
            apiKey: 'fc-test',
            apiUrl: 'https://api.firecrawl.dev',
        });
        mockFetch.mockResolvedValue({
            ok: true,
            status: 200,
            json: async () => ({
                answer: 'Try increasing waitFor.',
                fixParameters: { waitFor: 5000 },
            }),
        });
        const exitCode = await (0, doctor_1.runSupportAsk)({
            jobId,
            query: 'why did this scrape return empty markdown?',
        });
        (0, vitest_1.expect)(exitCode).toBe(0);
        (0, vitest_1.expect)(mockFetch).toHaveBeenCalledWith('https://api.firecrawl.dev/v2/support/ask', vitest_1.expect.objectContaining({
            method: 'POST',
            headers: vitest_1.expect.objectContaining({
                Authorization: 'Bearer fc-test',
                'Content-Type': 'application/json',
            }),
            body: JSON.stringify({
                question: 'why did this scrape return empty markdown?',
                jobId,
            }),
        }));
    });
    (0, vitest_1.it)('does not call support Ask without an API key', async () => {
        const exitCode = await (0, doctor_1.runSupportAsk)({ jobId });
        (0, vitest_1.expect)(exitCode).toBe(1);
        (0, vitest_1.expect)(mockFetch).not.toHaveBeenCalled();
    });
});
//# sourceMappingURL=doctor.test.js.map