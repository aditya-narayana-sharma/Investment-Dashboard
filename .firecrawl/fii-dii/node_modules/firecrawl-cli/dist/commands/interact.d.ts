/**
 * Interact command implementation
 * Execute AI prompts or code against a scraped page in a live browser session
 */
export interface InteractExecuteOptions {
    scrapeId?: string;
    prompt?: string;
    code?: string;
    language?: 'python' | 'node' | 'bash';
    timeout?: number;
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
export interface InteractStopOptions {
    scrapeId?: string;
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
/**
 * Execute a prompt or code in an interactive browser session bound to a scrape
 */
export declare function handleInteractExecute(options: InteractExecuteOptions): Promise<void>;
/**
 * Stop an interactive browser session bound to a scrape
 */
export declare function handleInteractStop(options: InteractStopOptions): Promise<void>;
//# sourceMappingURL=interact.d.ts.map