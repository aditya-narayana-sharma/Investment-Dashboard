"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
const vitest_1 = require("vitest");
const web_defaults_1 = require("../../utils/web-defaults");
const originalHome = process.env.HOME;
let tempHome;
async function read(relativePath) {
    return fs_1.promises.readFile(path_1.default.join(tempHome, relativePath), 'utf8');
}
async function write(relativePath, content) {
    const filePath = path_1.default.join(tempHome, relativePath);
    await fs_1.promises.mkdir(path_1.default.dirname(filePath), { recursive: true });
    await fs_1.promises.writeFile(filePath, content, 'utf8');
}
(0, vitest_1.describe)('configureWebDefaults', () => {
    (0, vitest_1.beforeEach)(async () => {
        tempHome = await fs_1.promises.mkdtemp(path_1.default.join(os_1.default.tmpdir(), 'firecrawl-web-'));
        process.env.HOME = tempHome;
    });
    (0, vitest_1.afterEach)(async () => {
        if (originalHome === undefined)
            delete process.env.HOME;
        else
            process.env.HOME = originalHome;
        await fs_1.promises.rm(tempHome, { recursive: true, force: true });
    });
    (0, vitest_1.it)('disables native Claude Code and Codex web tools', async () => {
        const results = await (0, web_defaults_1.configureWebDefaults)();
        (0, vitest_1.expect)(results.map((result) => result.changed)).toEqual([true, true]);
        (0, vitest_1.expect)(JSON.parse(await read('.claude/settings.json'))).toEqual({
            permissions: {
                deny: ['WebSearch', 'WebFetch'],
            },
        });
        (0, vitest_1.expect)(await read('.codex/config.toml')).toBe('web_search = "disabled"\n');
    });
    (0, vitest_1.it)('preserves existing Claude permissions and Codex config while disabling web', async () => {
        await write('.claude/settings.json', JSON.stringify({
            permissions: {
                allow: ['Read'],
                deny: ['Bash(rm *)', 'WebSearch'],
            },
        }));
        await write('.codex/config.toml', 'model = "gpt-5"\nweb_search = "cached"\n');
        await (0, web_defaults_1.configureWebDefaults)();
        (0, vitest_1.expect)(JSON.parse(await read('.claude/settings.json'))).toEqual({
            permissions: {
                allow: ['Read'],
                deny: ['Bash(rm *)', 'WebSearch', 'WebFetch'],
            },
        });
        (0, vitest_1.expect)(await read('.codex/config.toml')).toBe('model = "gpt-5"\nweb_search = "disabled"\n');
    });
    (0, vitest_1.it)('undoes only the native web defaults', async () => {
        await write('.claude/settings.json', JSON.stringify({
            permissions: {
                deny: ['Bash(rm *)', 'WebSearch', 'WebFetch'],
            },
        }));
        await write('.codex/config.toml', 'model = "gpt-5"\nweb_search = "disabled"\n');
        const results = await (0, web_defaults_1.configureWebDefaults)({ undo: true });
        (0, vitest_1.expect)(results.map((result) => result.changed)).toEqual([true, true]);
        (0, vitest_1.expect)(JSON.parse(await read('.claude/settings.json'))).toEqual({
            permissions: {
                deny: ['Bash(rm *)'],
            },
        });
        (0, vitest_1.expect)(await read('.codex/config.toml')).toBe('model = "gpt-5"\n');
    });
    (0, vitest_1.it)('writes Codex web_search at the root before TOML tables', async () => {
        await write('.codex/config.toml', 'model = "gpt-5"\n\n[mcp_servers.firecrawl]\ncommand = "npx"\n');
        await (0, web_defaults_1.configureWebDefaults)();
        (0, vitest_1.expect)(await read('.codex/config.toml')).toBe('model = "gpt-5"\n\nweb_search = "disabled"\n[mcp_servers.firecrawl]\ncommand = "npx"\n');
    });
    (0, vitest_1.it)('does not undo table-local Codex web_search settings', async () => {
        await write('.codex/config.toml', 'model = "gpt-5"\n\n[profiles.research]\nweb_search = "disabled"\n');
        await (0, web_defaults_1.configureWebDefaults)({ undo: true });
        (0, vitest_1.expect)(await read('.codex/config.toml')).toBe('model = "gpt-5"\n\n[profiles.research]\nweb_search = "disabled"\n');
    });
});
//# sourceMappingURL=web-defaults.test.js.map