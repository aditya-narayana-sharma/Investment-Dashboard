# Rejected integrations

These are documented so they are never stubbed as live:

- Unofficial Grow / Upstox reverse-engineering. Grow is `unavailable`; optional CSV is `cached` only.
- ET Prime / Moneycontrol login scraping. Use a user mailbox or a local PDF folder you already own.
- DriverKit, ARKit, HomeKit, LiveKit UI.
- Changing `?view=investment` to `portfolio-overview`.
- Cloud hosting of Mail, Health, or Kite tokens.
