"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.SEARCH_FEEDBACK_OPT_OUT_ENV_VARS = void 0;
exports.isSearchFeedbackDisabledLocally = isSearchFeedbackDisabledLocally;
exports.executeSearchFeedback = executeSearchFeedback;
exports.handleSearchFeedbackCommand = handleSearchFeedbackCommand;
exports.parseValuableSourcesArg = parseValuableSourcesArg;
exports.parseMissingContentArg = parseMissingContentArg;
const config_1 = require("../utils/config");
const client_1 = require("../utils/client");
exports.SEARCH_FEEDBACK_OPT_OUT_ENV_VARS = [
    'FIRECRAWL_NO_SEARCH_FEEDBACK',
    'FIRECRAWL_DISABLE_SEARCH_FEEDBACK',
];
const TRUTHY = new Set(['1', 'true', 'yes', 'on']);
function isSearchFeedbackDisabledLocally(env = process.env) {
    for (const key of exports.SEARCH_FEEDBACK_OPT_OUT_ENV_VARS) {
        const value = env[key];
        if (typeof value === 'string' && TRUTHY.has(value.trim().toLowerCase())) {
            return true;
        }
    }
    return false;
}
const DEFAULT_API_URL = 'https://api.firecrawl.dev';
async function executeSearchFeedback(options) {
    if (isSearchFeedbackDisabledLocally()) {
        return {
            success: true,
            disabled: true,
            disabledSource: 'env',
            creditsRefunded: 0,
            warning: 'Search feedback disabled by FIRECRAWL_NO_SEARCH_FEEDBACK; no data was sent.',
        };
    }
    try {
        if (options.apiKey || options.apiUrl) {
            (0, client_1.getClient)({ apiKey: options.apiKey, apiUrl: options.apiUrl });
        }
        const config = (0, config_1.getConfig)();
        const apiKey = options.apiKey || config.apiKey;
        (0, config_1.validateConfig)(apiKey);
        const apiUrl = (options.apiUrl || config.apiUrl || DEFAULT_API_URL).replace(/\/$/, '');
        const url = `${apiUrl}/v2/search/${encodeURIComponent(options.searchId)}/feedback`;
        const body = {
            rating: options.rating,
            origin: 'cli',
            integration: 'cli',
        };
        if (options.valuableSources && options.valuableSources.length > 0) {
            body.valuableSources = options.valuableSources
                .filter((s) => !!s.url)
                .map((s) => ({
                url: s.url,
                ...(s.reason ? { reason: s.reason } : {}),
            }));
        }
        if (options.missingContent && options.missingContent.length > 0) {
            body.missingContent = options.missingContent
                .filter((m) => !!m.topic)
                .map((m) => ({
                topic: m.topic,
                ...(m.description ? { description: m.description } : {}),
            }));
        }
        if (options.querySuggestions) {
            body.querySuggestions = options.querySuggestions;
        }
        const response = await fetch(url, {
            method: 'POST',
            headers: {
                Authorization: `Bearer ${apiKey}`,
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(body),
        });
        const data = await response
            .json()
            .catch(() => ({}));
        if (!response.ok || data?.success !== true) {
            const errorMessage = (typeof data?.error === 'string' && data.error) ||
                `HTTP ${response.status}: ${response.statusText}`;
            const errorCode = typeof data?.feedbackErrorCode === 'string'
                ? data.feedbackErrorCode
                : undefined;
            if (errorCode === 'TEAM_OPTED_OUT') {
                return {
                    success: true,
                    disabled: true,
                    disabledSource: 'team',
                    creditsRefunded: 0,
                    warning: 'Search feedback is disabled for this team. Contact support to re-enable.',
                };
            }
            return { success: false, error: errorMessage, errorCode };
        }
        return {
            success: true,
            feedbackId: data.feedbackId,
            creditsRefunded: data.creditsRefunded ?? 0,
            creditsRefundedToday: typeof data.creditsRefundedToday === 'number'
                ? data.creditsRefundedToday
                : undefined,
            dailyRefundCap: typeof data.dailyRefundCap === 'number'
                ? data.dailyRefundCap
                : undefined,
            dailyCapReached: data.dailyCapReached === true,
            alreadySubmitted: data.alreadySubmitted,
            warning: data.warning,
        };
    }
    catch (error) {
        return {
            success: false,
            error: error?.message || 'Unknown error occurred',
        };
    }
}
function formatReadable(result) {
    const lines = [];
    if (result.alreadySubmitted) {
        lines.push(`Feedback already submitted for this search.`);
    }
    else {
        lines.push(`Feedback recorded.`);
    }
    if (result.feedbackId) {
        lines.push(`Feedback ID: ${result.feedbackId}`);
    }
    lines.push(`Credits refunded: ${result.creditsRefunded ?? 0}`);
    if (typeof result.creditsRefundedToday === 'number' &&
        typeof result.dailyRefundCap === 'number') {
        lines.push(`Refunds today: ${result.creditsRefundedToday} / ${result.dailyRefundCap}`);
    }
    if (result.dailyCapReached) {
        lines.push('Daily refund cap reached — further /feedback calls today will not refund credits.');
    }
    if (result.warning) {
        lines.push(`Warning: ${result.warning}`);
    }
    return lines.join('\n') + '\n';
}
async function handleSearchFeedbackCommand(options) {
    const result = await executeSearchFeedback(options);
    if (result.disabled) {
        if (options.silent) {
            process.exit(0);
        }
        if (result.disabledSource === 'env') {
            console.error('Search feedback is disabled (FIRECRAWL_NO_SEARCH_FEEDBACK is set). ' +
                'Nothing was sent. Unset the env var to re-enable.');
        }
        else {
            console.error(result.warning ?? 'Search feedback is disabled for this team.');
        }
        process.exit(0);
    }
    if (!result.success) {
        // --silent always exits 0 so background pipelines don't crash; only
        // surface 5xx-class errors to stderr when explicit debugging is on.
        if (options.silent) {
            const noisy = ['INTERNAL', 'DB_DISABLED'];
            if (result.errorCode &&
                noisy.includes(result.errorCode) &&
                process.env.FIRECRAWL_SEARCH_FEEDBACK_DEBUG === '1') {
                console.error(`firecrawl search-feedback: ${result.errorCode}: ${result.error}`);
            }
            process.exit(0);
        }
        console.error('Error:', result.error);
        if (result.errorCode) {
            console.error(`Code: ${result.errorCode}`);
        }
        process.exit(1);
    }
    if (options.silent) {
        return;
    }
    let outputContent;
    if (options.json || options.pretty) {
        const json = {
            success: true,
            feedbackId: result.feedbackId,
            creditsRefunded: result.creditsRefunded ?? 0,
            ...(typeof result.creditsRefundedToday === 'number'
                ? { creditsRefundedToday: result.creditsRefundedToday }
                : {}),
            ...(typeof result.dailyRefundCap === 'number'
                ? { dailyRefundCap: result.dailyRefundCap }
                : {}),
            ...(result.dailyCapReached ? { dailyCapReached: true } : {}),
            ...(result.alreadySubmitted ? { alreadySubmitted: true } : {}),
            ...(result.warning ? { warning: result.warning } : {}),
        };
        outputContent = options.pretty
            ? JSON.stringify(json, null, 2)
            : JSON.stringify(json);
    }
    else {
        outputContent = formatReadable(result);
    }
    if (options.output) {
        const fs = await Promise.resolve().then(() => __importStar(require('fs')));
        const path = await Promise.resolve().then(() => __importStar(require('path')));
        const dir = path.dirname(options.output);
        if (dir && !fs.existsSync(dir)) {
            fs.mkdirSync(dir, { recursive: true });
        }
        fs.writeFileSync(options.output, outputContent, 'utf-8');
        console.error(`Output written to: ${options.output}`);
    }
    else {
        if (!outputContent.endsWith('\n'))
            outputContent += '\n';
        process.stdout.write(outputContent);
    }
}
function parseValuableSourcesArg(raw) {
    if (!raw)
        return undefined;
    const trimmed = raw.trim();
    if (!trimmed)
        return undefined;
    if (trimmed.startsWith('[') || trimmed.startsWith('{')) {
        try {
            const parsed = JSON.parse(trimmed);
            const arr = Array.isArray(parsed) ? parsed : [parsed];
            const cleaned = [];
            for (const entry of arr) {
                if (entry &&
                    typeof entry === 'object' &&
                    typeof entry.url === 'string') {
                    cleaned.push({
                        url: entry.url,
                        ...(typeof entry.reason === 'string'
                            ? { reason: entry.reason }
                            : {}),
                    });
                }
                else if (typeof entry === 'string') {
                    cleaned.push({ url: entry });
                }
            }
            return cleaned.length > 0 ? cleaned : undefined;
        }
        catch {
            throw new Error('--valuable-sources must be valid JSON or a comma-separated list of URLs.');
        }
    }
    return trimmed
        .split(',')
        .map((u) => u.trim())
        .filter((u) => u.length > 0)
        .map((url) => ({ url }));
}
// Accepts JSON arrays/objects, "topic: description" strings, comma-
// separated topic lists, or repeated values. Caps at 20 entries.
function parseMissingContentArg(raw) {
    if (!raw)
        return undefined;
    const inputs = Array.isArray(raw) ? raw : [raw];
    const out = [];
    for (const value of inputs) {
        if (typeof value !== 'string')
            continue;
        const trimmed = value.trim();
        if (!trimmed)
            continue;
        if (trimmed.startsWith('[') || trimmed.startsWith('{')) {
            try {
                const parsed = JSON.parse(trimmed);
                const arr = Array.isArray(parsed) ? parsed : [parsed];
                for (const entry of arr) {
                    if (typeof entry === 'string') {
                        const topic = entry.trim();
                        if (topic)
                            out.push({ topic });
                    }
                    else if (entry &&
                        typeof entry === 'object' &&
                        typeof entry.topic === 'string') {
                        const topic = entry.topic.trim();
                        if (!topic)
                            continue;
                        const description = typeof entry.description === 'string'
                            ? entry.description.trim()
                            : undefined;
                        out.push({
                            topic,
                            ...(description ? { description } : {}),
                        });
                    }
                }
                continue;
            }
            catch {
                throw new Error('--missing-content must be valid JSON, "topic: description" form, or a comma-separated topic list.');
            }
        }
        for (const part of trimmed
            .split(',')
            .map((p) => p.trim())
            .filter(Boolean)) {
            const colonIdx = part.indexOf(':');
            if (colonIdx > 0) {
                const topic = part.slice(0, colonIdx).trim();
                const description = part.slice(colonIdx + 1).trim();
                if (topic) {
                    out.push({
                        topic,
                        ...(description ? { description } : {}),
                    });
                }
            }
            else {
                out.push({ topic: part });
            }
        }
    }
    if (out.length === 0)
        return undefined;
    return out.slice(0, 20);
}
//# sourceMappingURL=search-feedback.js.map