/**
 * Scrape command implementation
 */
import type { ScrapeOptions, ScrapeResult } from '../types/scrape';
/**
 * Execute the scrape command
 */
export declare function executeScrape(options: ScrapeOptions): Promise<ScrapeResult>;
/**
 * Handle scrape command output
 */
export declare function handleScrapeCommand(options: ScrapeOptions): Promise<void>;
/**
 * Handle scrape for multiple URLs.
 * Each result is saved as a separate file in .firecrawl/
 */
export declare function handleMultiScrapeCommand(urls: string[], options: ScrapeOptions): Promise<void>;
/**
 * Map an entire site and scrape all discovered URLs.
 * Organizes results into nested directories based on URL paths.
 */
interface AllScrapeOptions {
    limit?: number;
    yes?: boolean;
    search?: string;
    includePaths?: string[];
    excludePaths?: string[];
    allowSubdomains?: boolean;
}
export declare function handleAllScrapeCommand(siteUrl: string, options: ScrapeOptions, allOptions?: AllScrapeOptions): Promise<void>;
export {};
//# sourceMappingURL=scrape.d.ts.map