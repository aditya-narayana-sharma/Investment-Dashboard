/**
 * Browser command implementation
 * Manages cloud browser sessions via the Firecrawl SDK
 */
export interface BrowserLaunchOptions {
    ttl?: number;
    ttlInactivity?: number;
    profile?: string;
    saveChanges?: boolean;
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
export interface BrowserExecuteOptions {
    code: string;
    language?: 'python' | 'node' | 'bash';
    session?: string;
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
export interface BrowserListOptions {
    status?: 'active' | 'destroyed';
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
export interface BrowserCloseOptions {
    session?: string;
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
export interface BrowserQuickExecuteOptions {
    code: string;
    profile?: string;
    saveChanges?: boolean;
    apiKey?: string;
    apiUrl?: string;
    output?: string;
    json?: boolean;
}
/**
 * Launch a new browser session
 */
export declare function handleBrowserLaunch(options: BrowserLaunchOptions): Promise<void>;
/**
 * Execute a bash command locally with CDP_URL and SESSION_ID env vars.
 * agent-browser reads CDP_URL from the environment automatically.
 */
export declare function executeBashLocally(command: string, session: {
    id: string;
    cdpUrl: string;
}): Promise<{
    stdout: string;
    stderr: string;
    exitCode: number;
}>;
/**
 * Execute code in a browser session
 */
export declare function handleBrowserExecute(options: BrowserExecuteOptions): Promise<void>;
/**
 * List browser sessions
 */
export declare function handleBrowserList(options: BrowserListOptions): Promise<void>;
/**
 * Shorthand: auto-launch a session if needed, then execute an agent-browser command.
 * Enables `firecrawl browser "open example.com"` without a separate launch step.
 */
export declare function handleBrowserQuickExecute(options: BrowserQuickExecuteOptions): Promise<void>;
/**
 * Close a browser session
 */
export declare function handleBrowserClose(options: BrowserCloseOptions): Promise<void>;
//# sourceMappingURL=browser.d.ts.map