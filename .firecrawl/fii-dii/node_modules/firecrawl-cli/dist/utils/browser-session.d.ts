/**
 * Browser session persistence utility
 * Stores active browser session info in platform-specific config directory
 */
export interface StoredBrowserSession {
    id: string;
    cdpUrl: string;
    createdAt: string;
}
/**
 * Save active browser session to disk
 */
export declare function saveBrowserSession(session: StoredBrowserSession): void;
/**
 * Load stored browser session from disk
 */
export declare function loadBrowserSession(): StoredBrowserSession | null;
/**
 * Clear stored browser session from disk
 */
export declare function clearBrowserSession(): void;
/**
 * Resolve session ID from override flag or stored session
 */
export declare function getSessionId(overrideId?: string): string;
//# sourceMappingURL=browser-session.d.ts.map